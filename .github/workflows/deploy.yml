name: Deploy to reg.ru

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: FTP Deploy to reg.ru
        uses: SamKirkland/FTP-Deploy-Action@v4
        with:
          server: 31.31.196.45
          username: u3458917
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: /www/archetypeos.ru/
          local-dir: ./
          exclude: |
            **/.git/**
            **/.gitattributes
            **/.github/**
            README.md
